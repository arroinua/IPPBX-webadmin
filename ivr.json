/*
	"name": имя IVR
	"greetings": приветствие IVR
	"mainaudio": голосовое файл главного меню
	"tries": попыток ввода цифры до того, как вызово будет разъединен
	"back": кнопка перехода в предидущее меню
	"repeat": кнопка повтора звукового файла
	"tomain": кнопка перехода в главное меню
	"timeout": таймаут ввода цифры
	"commutators": [ коммутаторы
		{
			"name": имя коммутатора
			"number": номер коммутации
		}
	],
	"objects": [ объекты
		{
			"pid": id родительского меню (у главного меню он будет равен 0)
			"type": тип объекта меню. Может быть
					1 - "menu"
					2 - "commutator"					
			"name": имя объекта
			"audio": голосовой файл меню - только для типа "menu"
			"cname": имя коммутатора - только для типа "commutator"
			"button": цифра перехода в меню
			"oid": id объекта. У главного меню он будет равен 0. У остальных объектов id формируется следущим образом:
					oid меню в котором находится объект + значение параметра button в данном объекте.
		}
	]
*/

{
	"name": "string",
	"greetings": "string",
	"mainaudio": "string",
	"tries": "number",
	"back": "number",
	"repeat": "number",
	"tomain": "number",
	"timeout": "number",
	"commutators": [
		{
			"name": "string",
			"number": "string"
		}
	],
	"objects": [
		{
			"pid": "string",
			"type": "number",
			"oid": "string",
			"name": "string",
			"audio": "string",
			"cname": "string",
			"button": "number"
		}
	]
}

/*
Files format

Нужно создать два текстовых файла в формате txt. Названия могут быть любыми.
В файле connectors.txt нужно построчно записывать параметр "number" созданных коммутаторов.
Например:
connector.txt

0663149219
0507093357
1009
1100
112
0503682580

*********************

В файл menulist.txt нужно построчно записывать совокупность oid объектов типа "commutator", у которых параметр "cname" равен параметру "name" коммутатора.
Например:
objects.txt

12,123,42,35,44,01,05,
11,114,15,23,52,64,
13,131,19,36,40,
02,18,20,
55,

*/